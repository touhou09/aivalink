import { describe, it } from "vitest";

describe.skip("BillingRepository (sqlite legacy tests)", () => {
  it("is temporarily skipped after postgres migration", () => {
    // TODO: rewrite this suite for PostgresManager test harness
  });
});
